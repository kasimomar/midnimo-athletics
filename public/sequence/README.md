# Legacy image-sequence directory

This directory is left over from the original starter. The current Midnimo Athletics hero uses an optional HTML video configured with `NEXT_PUBLIC_HERO_VIDEO_URL`, with a gradient fallback. It does not load frame images from this directory. See the root README and `docs/configuration.md`.
